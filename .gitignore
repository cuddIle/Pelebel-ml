# Datasets (too large for git — kept locally only)
dataset/*
dataset/*/*
!dataset/.gitkeep
!dataset/*/.gitkeep

landmarks-dataset/*
!landmarks-dataset/.gitkeep

# Python
__pycache__/
*.py[cod]

# Virtual environment
.venv/
